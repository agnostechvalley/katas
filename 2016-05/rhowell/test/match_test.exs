defmodule MatchTest do
  use ExUnit.Case

  test "Given a hand with matching suits and all high cards" do
#    assert :royal_flush == Match.get_type(MapSet.new([{:club, :ace},
              #                                    {:club, :king},
            #                                      {:club, :queen},
            #                                      {:club, :jack},
            #                                      {:club, :ten}]))
  end
end
